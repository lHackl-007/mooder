# Android 通用漏洞扫描平台商业化改造方案

## 1. 背景与目标

当前项目的核心问题，不是“能不能扫出一些问题”，而是“扫出来的问题能不能直接拿去核实、复现、提交、交付”。  
真正的商业级移动安全扫描平台，必须解决以下痛点：

- 不能只给命中点，必须给完整上下文与调用链证明。
- 不能只给静态猜测，必须尽可能在本地真机上自动验证。
- 不能把“攻击面”“疑似风险”“真实漏洞”混在一起报告。
- 不能只适配某一个 App，必须针对任意 Android App 具备较强通用性。
- 对于非 Root 和 Root 两种手机环境，必须有不同的验证与报告策略。
- 对于无法自动验证但代码上已能确定成立的问题，必须给出明确的代码证明和可执行复现步骤。

本方案的目标，是将现有项目升级为一套面向多 App、强调高准确率、高证据密度、可本机验证、可生成小白可复现报告的 Android 安全扫描平台。

## 2. 最终目标能力定义

平台最终应具备以下能力：

1. 输入任意 Android APK 或已安装包名，自动完成静态审计。
2. 自动识别导出组件、Deeplink、WebView、JSBridge、Provider、安装下载链、鉴权类逻辑、隐私读取链等核心攻击面。
3. 对每个问题输出“入口 -> 参数 -> 处理逻辑 -> 危险动作”的完整调用链。
4. 自动区分：
   - 已动态验证的真实漏洞
   - 代码链闭环可确认的问题
   - 需要 Root 才能验证的问题
   - 仅攻击面，不应作为正式漏洞提交的问题
5. 在手机连接情况下，自动完成：
   - ADB 非 Root 验证
   - Root 环境 Frida/Hook 验证
   - WebView/JSBridge PoC 注入验证
   - Provider 数据返回验证
   - 导出组件启动与副作用验证
6. 自动生成适合：
   - 内部审计
   - SRC/HackerOne 风格提交
   - 小白复现
   的标准化漏洞报告。

## 3. 核心设计原则

### 3.1 证据优先，而不是命中优先

任何漏洞结论都必须有足够证据支撑。  
“扫到敏感方法”不等于漏洞。  
“组件导出”不等于漏洞。  
“有 JSBridge”也不等于漏洞。

平台必须优先保证以下判断顺序：

1. 是否存在外部可达入口
2. 是否可控关键参数
3. 是否进入敏感逻辑
4. 是否触发真实危险动作
5. 是否已在真机环境验证

只有完成 1-4，才能进入“代码证实”；完成 1-5，才能进入“已验证漏洞”。

### 3.2 分级输出，而不是一锅端

平台必须强制将问题划分为不同证据等级，严禁混报。

建议固定分桶如下：

- `VERIFIED_REAL`
  已在本地设备验证成功，具备可重复证据。
- `CODE_PROVEN`
  未必已在当前设备完成动态复现，但通过完整代码链可确定漏洞成立。
- `ROOT_VERIFIED`
  需 Root 场景才能动态验证，且已在 Root 设备验证成功。
- `ROOT_REQUIRED`
  需 Root 才能验证，当前仅完成代码证明并生成可直接运行的 Root 脚本。
- `ATTACK_SURFACE_ONLY`
  仅有攻击面，不足以单独作为正式漏洞。

正式报告默认仅展示：

- `VERIFIED_REAL`
- `CODE_PROVEN`
- `ROOT_VERIFIED`

其余问题进入候选区或研究区。

### 3.3 静态分析与动态验证必须联动

静态分析负责“发现路径”，动态验证负责“确认结果”。  
没有动态验证能力的扫描平台，最终只能停留在半自动审计工具层面。

### 3.4 必须支持 Root 与非 Root 两种运行模式

Root 和非 Root 下的能力差异很大，平台必须原生支持双模式：

- 非 Root：
  ADB、deeplink、content、broadcast、PoC 页面、logcat、界面观察
- Root：
  Frida Hook、进程内方法观察、参数拦截、返回值抓取、私有数据访问验证、鉴权绕过验证

## 4. 平台总体架构

建议平台分为八个核心模块：

1. 输入与环境模块
2. 反编译与预处理模块
3. 静态分析引擎
4. 调用链与证据图引擎
5. 动态验证引擎
6. Root 能力引擎
7. 证据存储与任务编排模块
8. 报告生成与 Web 展示模块

下面逐一展开。

## 5. 输入与环境模块

### 5.1 输入类型

平台应支持以下输入：

- APK 文件路径
- 本机已安装包名
- ADB 连接设备上的包名
- 批量 APK 目录
- 批量包名列表

### 5.2 环境探测

扫描开始前，自动探测：

- 是否存在 ADB
- 是否连接设备
- 当前设备序列号
- 设备 Android 版本
- 目标包是否安装
- 是否具备 Root
- `su` 是否可用
- 是否存在 Frida Server 或可注入 Frida
- 是否可用端口映射 `adb reverse`
- 是否可写本地 PoC 目录

环境探测结果必须直接进入报告头部，作为所有验证结论的上下文。

## 6. 反编译与预处理模块

### 6.1 标准化反编译链

不能依赖单一反编译器。建议并行使用：

- `jadx`
  生成 Java 语义视图，便于规则与上下文阅读
- `apktool`
  生成 Manifest、资源、smali，便于确认编译器丢失逻辑
- `aapt/aapt2`
  获取包信息、组件信息、权限等

### 6.2 预处理产物

预处理后生成统一中间目录结构：

- `manifest/`
- `jadx_sources/`
- `smali/`
- `resources/`
- `signatures/`
- `metadata/`

### 6.3 中间表示 IR

必须构建统一 IR，而不是让规则直接扫文本。  
IR 至少要抽象出：

- 组件
- Intent Filter
- Uri 结构
- 方法定义
- 调用关系
- 参数来源
- 参数传播
- 敏感汇点
- 权限检查点
- WebView/JSBridge 注册点

这是从“脚本工具”走向“平台”的关键一步。

## 7. 静态分析引擎

静态分析引擎负责构造攻击面和漏洞候选，不直接下最终结论。

### 7.1 Manifest 与导出面分析

必须识别：

- Exported Activity
- Exported Service
- Exported Receiver
- Exported Provider
- 自定义权限保护
- `intent-filter`
- `scheme/host/path/pathPattern`
- `grantUriPermissions`
- `readPermission/writePermission`

并形成统一攻击面数据结构：

- 入口组件名
- 外部调用方式
- 所需权限
- 是否默认外部可达
- 参数来源类型

### 7.2 Deeplink / AppLink 分析

核心目标是识别：

- 自定义 Scheme 路由
- 入口参数
- `entryType` / `url` / `URL` / `target` / `class` / `pkg`
- 白名单校验逻辑
- 重定向与二次跳转风险
- 进入 WebView/XWebView 的链路

这里不能只记录某一行 `getQueryParameter("URL")`，必须继续追踪：

- 校验逻辑
- 白名单函数
- URL 解析
- 跳转目标类
- 最终 `startActivity/loadUrl`

### 7.3 WebView / XWebView 分析

需要识别：

- `setJavaScriptEnabled(true)`
- `loadUrl()`
- `shouldOverrideUrlLoading()`
- `setAllowFileAccess()`
- `setAllowUniversalAccessFromFileURLs()`
- `addJavascriptInterface()`
- URL 白名单校验
- 标题栏控制、记录开关、回调参数

分析重点不是“WebView 存在”，而是：

- 外部是否可控加载 URL
- 是否能进入受 JSBridge 保护的 Web 页面
- 是否能借助真实白名单或跳转页面绕过限制

### 7.4 JSBridge 分析

对每个 `@JavascriptInterface` 方法，必须自动归类危险能力：

- 读取设备信息
- 读取安装应用列表
- 启动任意 App
- 启动任意组件
- 打开浏览器
- 发起下载
- 发起安装
- 读写账户数据
- 查询积分/钱包/订单类数据

同时自动输出：

- 注册对象名，例如 `android`
- 注册类
- 所属 WebView 类
- 加载页面来源
- 调用前提
- 可利用影响

### 7.5 Provider 分析

不能只看导出，还要看：

- `query/insert/update/delete/call/openFile`
- UriMatcher 路由
- 是否校验调用包
- 是否校验签名
- 是否依赖固定字段生成签名
- 是否返回明确数据
- 是否支持 `content query` 直接取数

Provider 必须单独区分：

- 仅入口存在
- 可读数据
- 可写数据
- 可触发副作用

### 7.6 导出组件危险动作分析

对导出组件要继续追踪：

- 启动后是否读取外部参数
- 是否进入下载/安装
- 是否进入付款/转账/删除/发消息/拨号等危险动作
- 是否只打开无意义界面

如果只是打开一个正常首页或设置页，不能直接报漏洞。

### 7.7 安装/下载链分析

这是应用商店类 App 的重点。

要识别：

- APK URL 来源
- 下载文件路径
- `PackageInstaller` 交互
- Session 流程
- Silent install 前提
- 安装确认页面拦截逻辑
- 来源校验逻辑
- Referrer / callingPackage 校验

### 7.8 鉴权与逻辑越权分析

这个模块不能再是简单字符串规则。

必须做“同类方法对比”：

- 识别同类敏感方法集合
- 统计其中哪些方法存在：
  `isOwner/isAdmin/checkPermission/loginCheck/roleCheck`
- 找出未做校验但执行同类危险动作的方法
- 再追踪该方法是否可被外部触达或业务可达

若无业务可达入口，仅作为研究项，不进正式漏洞。

## 8. 调用链与证据图引擎

这是当前项目最缺、也是最关键的一层。

### 8.1 为什么必须有调用链

用户真正需要的不是“命中了哪一行”，而是：

- 谁可以调用
- 参数从哪里来
- 中间经过哪些处理
- 最后触发了什么危险动作

### 8.2 最小证据链模型

每个漏洞至少生成以下节点：

1. 入口节点
2. 参数节点
3. 条件判断节点
4. 中间转换节点
5. 敏感动作节点
6. 验证节点

例如：

`Exported Activity -> getData() -> getQueryParameter("URL") -> SafeDeepLinkHandler.checkExecWebIsSafe() -> WebViewActivity.putExtra("WebSite") -> loadUrl() -> addJavascriptInterface("android")`

### 8.3 调用链输出要求

报告中必须自动写出：

- 类名
- 方法名
- 参数名
- 关键判断
- 最终动作
- 代码片段

不能只贴 3 行孤立代码。

## 9. 动态验证引擎

动态验证是商业化平台的分水岭。

### 9.1 非 Root 验证能力

非 Root 下应支持自动：

- `adb shell am start`
- `adb shell am broadcast`
- `adb shell am startservice`
- `adb shell content query/insert/update/delete/call`
- `adb reverse`
- 启动本地 PoC HTTP 服务
- 注入测试网页
- 记录 logcat
- 判断 Activity 是否实际启动
- 判断是否有副作用

### 9.2 Root 验证能力

Root 下增加：

- `su -c` 命令执行
- Frida 注入
- Hook 关键方法入参/返回值
- Hook 鉴权分支
- Hook WebView URL 加载
- Hook JSBridge 调用
- Hook Provider 返回
- Hook 安装/下载/删除/账户操作等关键函数

### 9.3 验证脚本自动生成

平台不能只告诉用户“需要 Hook”，而必须自动给出：

- ADB 命令
- Frida 脚本
- 本地 PoC HTML
- HTTP 服务启动方式
- Root 命令
- 数据抓取方法

### 9.4 验证结果记录

每次动态验证都必须产出证据文件：

- 执行命令
- 返回状态
- 日志片段
- Hook 输出
- 截图
- 页面抓取结果

这些证据必须与漏洞 ID 绑定，后续报告直接引用。

## 10. Root 与非 Root 的漏洞处理策略

### 10.1 非 Root 可直接验证的问题

例如：

- 导出 Activity/Service/Receiver
- 普通 Provider 读写
- Deeplink 跳 WebView
- 白名单绕过
- JSBridge 调用
- 外部安装下载链

这些问题若可验证，必须优先给出动态复现结果。

### 10.2 需要 Root 才能验证的问题

例如：

- 私有目录读取
- 数据库直读
- 需要 Hook 才能确认返回值的问题
- 鉴权逻辑绕过后的深层危险动作确认
- 某些系统权限路径验证

这类问题必须：

- 明确标记 `ROOT_REQUIRED` 或 `ROOT_VERIFIED`
- 给出完整脚本
- 给出预期结果
- 说明为什么 Root 是必要条件

### 10.3 无 Root 但代码可确定成立的问题

如果当前没有 Root，但代码已经能完整证明漏洞成立，也允许进入 `CODE_PROVEN`，前提是：

- 入口可达
- 参数可控
- 条件可满足
- 危险动作明确
- 不依赖纯猜测

并且必须给出手工复现步骤与脚本。

## 11. 漏洞评级与误报控制

### 11.1 为什么误报控制很重要

当前很多工具最大的问题不是“扫不到”，而是“乱报”。  
一旦报告里大量存在：

- 打开首页就是漏洞
- 导出组件就是漏洞
- 没证据就写高危

那整个平台会失去可信度。

### 11.2 漏洞判定准则

每个问题都必须回答：

1. 外部是否可达
2. 参数是否可控
3. 动作是否危险
4. 影响是否真实
5. 是否已验证

只要其中任意关键项不成立，就必须降级。

### 11.3 攻击面与正式漏洞分离

以下问题通常不应直接作为正式漏洞：

- 单纯导出 Activity
- 可打开首页
- 仅存在 JSBridge 但无法从外部进入
- 仅发现敏感方法但无可达路径
- 仅存在白名单逻辑但未发现绕过

这些只能进入攻击面或候选区。

## 12. 报告生成规范

### 12.1 报告结构

每份正式报告建议固定结构：

1. 扫描概况
2. 设备与环境
3. 发现漏洞汇总
4. 已验证漏洞详情
5. 代码证实漏洞详情
6. Root 专项漏洞详情
7. 候选问题与不收取问题
8. 证据附件索引

### 12.2 漏洞详情模板

每条漏洞固定包含：

- 标题
- 严重度
- 类型
- 是否需 Root
- 真实性等级
- 触发方式
- 影响条件
- 入口点
- 调用链
- 关键代码证明
- 动态验证证据
- 小白复现步骤
- 命令/脚本
- 预期结果
- 风险说明
- 修复建议

### 12.3 小白复现要求

复现部分必须做到：

- 不假设读者懂逆向
- 命令可直接复制执行
- Hook 脚本可直接运行
- 页面 PoC 可直接访问
- 每一步写明预期看到什么

## 13. Web 界面设计要求

Web 界面不是装饰层，而是工作台。

必须支持：

- 任务创建
- 单 App / 批量扫描
- 当前环境状态展示
- 扫描进度
- 静态发现面查看
- 调用链展开
- 证据文件查看
- 动态验证日志查看
- Root/非 Root 状态展示
- 报告预览
- 漏洞分桶过滤

### 13.1 漏洞详情页必须展示

- 入口组件
- 调用链图
- 命中代码上下文
- 动态日志
- Hook 输出
- 复现命令
- PoC 附件

### 13.2 禁止的展示方式

- 只显示一小段命中代码
- 不显示谁调用谁
- 不显示证据
- 不区分真实漏洞与攻击面

## 14. 数据模型设计

建议核心对象如下：

### 14.1 ScanTask

- 任务 ID
- 包名
- APK 路径
- 扫描时间
- 设备信息
- Root 状态
- 扫描状态

### 14.2 Finding

- 漏洞 ID
- 标题
- 类型
- 严重度
- 真实性等级
- 是否需 Root
- 是否动态验证
- 规则名
- 当前状态

### 14.3 Evidence

- 证据 ID
- 关联漏洞 ID
- 证据类型
- 文件路径
- 日志路径
- 截图路径
- 脚本路径

### 14.4 CodeTrace

- 源节点
- 目标节点
- 参数名
- 传播类型
- 关键条件

### 14.5 ValidationRun

- 验证 ID
- 验证方式
- 是否成功
- 设备
- Root 状态
- 命令
- 返回结果
- 证据输出

## 15. 通用规则体系设计

建议规则不再写成分散脚本，而是统一抽象。

### 15.1 规则分类

- `COMPONENT_EXPORTED`
- `DEEPLINK_TO_WEBVIEW`
- `JSBRIDGE_DANGEROUS_METHOD`
- `PROVIDER_DATA_EXPOSURE`
- `PROVIDER_WRITE_ACTION`
- `INSTALL_FLOW_EXTERNAL_CONTROL`
- `DOWNLOAD_URL_EXTERNAL_CONTROL`
- `INTENT_PARAMETER_INJECTION`
- `AUTHZ_INCONSISTENT_GUARD`
- `ROOT_PRIVATE_DATA_ACCESS`

### 15.2 规则运行结果

每条规则输出的不是漏洞，而是“候选链路”。

候选链路应包含：

- source
- sink
- guards
- evidence candidates
- validation plan

再由验证与证据层做最终收敛。

## 16. 自动验证模板库

平台应内置验证模板，不同漏洞自动选用。

### 16.1 Deeplink 模板

- 自动构造 `am start`
- 多参数变体
- 白名单域名变体
- 本地 PoC 页注入

### 16.2 Provider 模板

- `content query`
- `content insert`
- `content update`
- `content delete`
- 返回值抓取

### 16.3 JSBridge 模板

- 本地 HTML PoC
- `window.android` 能力探测
- 敏感接口调用
- 返回值输出

### 16.4 Root Hook 模板

- 方法入参打印
- 返回值打印
- 鉴权绕过验证
- 分支覆盖验证
- 数据抓取验证

## 17. 证据留存与可审计性

平台必须支持完整留痕。

每次扫描至少保留：

- 原始命中结果
- 代码上下文
- 传播链
- 执行命令
- ADB 返回
- Hook 日志
- 截图
- 生成的 PoC
- 报告版本

避免出现“扫描结束自动清理掉上下文”的问题。

## 18. 开发实施路线

建议分四期推进。

### 第一期：基础平台重构

目标：

- 统一输入层
- 统一反编译层
- 统一数据模型
- 统一报告数据结构

产出：

- 可稳定扫描任意 APK
- 基础攻击面列表

### 第二期：调用链与证据引擎

目标：

- 参数传播追踪
- 漏洞候选链生成
- 代码上下文自动收集

产出：

- 每个问题不再只有命中点，而有最小调用链

### 第三期：动态验证引擎

目标：

- 非 Root 验证模板
- Root 验证模板
- Frida 集成
- PoC 自动生成

产出：

- 自动复现一部分高价值漏洞

### 第四期：商业化报告与 Web 工作台

目标：

- 结构化报告
- Web 漏洞详情页
- 证据附件页
- 批量任务编排

产出：

- 可直接交付客户/研究员使用的工作台

## 19. 验收标准

一个版本是否合格，建议用以下标准验收：

### 19.1 静态分析验收

- 是否能识别导出组件
- 是否能识别 Deeplink 参数
- 是否能识别 WebView/JSBridge
- 是否能识别 Provider 路由
- 是否能输出调用链

### 19.2 动态验证验收

- 是否能自动启动目标组件
- 是否能自动构造 PoC
- 是否能自动抓取证据
- 是否能区分验证成功与未成功

### 19.3 报告验收

- 是否区分真实漏洞与攻击面
- 是否给出可执行命令
- 是否给出小白复现步骤
- 是否给出 Root/非 Root 条件说明
- 是否给出证据附件索引

## 20. 现实可达成的效果预估

在工程实现合理、设备条件完善、测试链路通畅的前提下，平台可达到以下现实水平：

- 攻击面发现率：80% - 90%
- 正式漏洞准确率：70% - 85%
- 高危漏洞准确率：85% 以上
- 非 Root 自动复现覆盖：50% - 70%
- Root 自动复现覆盖：70% - 85%
- 小白可直接复现报告覆盖：60% - 80%

这里强调的是“现实可达成”，而不是宣传级的“全自动 100%”。

## 21. 风险与限制

以下问题是客观存在的，必须在方案中提前承认：

- 混淆和动态加载会削弱静态追踪质量
- 业务需登录、邀请码、特定账号时，动态验证难度会提高
- 云端配置、灰度开关、服务端鉴权会影响本地复现
- 某些逻辑问题只能证明代码成立，未必能在当前设备状态复现
- 部分高危场景需要真实业务上下文，不适合全自动模拟

因此平台设计必须允许“代码证实但暂未动态复现”的中间状态存在。

## 22. 对现有项目的具体改造建议

如果基于现有项目继续做，建议优先改以下几项：

1. 建立统一 `Finding/Evidence/Trace/ValidationRun` 数据模型  
2. 重写漏洞输出流程，先出候选链，再做验证，再出最终结论  
3. 为每条规则补充“调用链上下文采集”能力  
4. 增加 Root 与非 Root 两套验证执行器  
5. 增加 PoC 自动生成器  
6. 重写报告模板，按真实性分桶输出  
7. Web 界面增加“证据页”“调用链页”“验证日志页”  
8. 批量扫描时保留每个漏洞的上下文证据，不自动清理  

## 23. 结论

要做成商业级 Android 安全扫描平台，核心不是“多加几个规则”，而是把项目从“文本命中式扫描器”升级为“证据驱动的半自动审计平台”。

真正应该建设的是一条完整流水线：

- 静态发现
- 调用链证明
- 本机验证
- Root 专项验证
- 证据归档
- 标准化报告

只有这样，最终输出的结果才会是：

- 对研究员有用
- 对 SRC 可提交
- 对小白可复现
- 对商业客户可交付

---

文档用途建议：

- 作为后续开发路线图
- 作为 Web 端字段设计依据
- 作为漏洞报告标准模板依据
- 作为规则重构与验证引擎重构的总纲
